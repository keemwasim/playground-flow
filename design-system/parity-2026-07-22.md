# parity and tells sweep, 2026-07-22 (v4 hardening pass)

Scope: design-system/ (tokens, styles.css, all 23 component triplets, README, SKILL, CHANGELOG), Design System Spec.dc.html, Component Gallery.dc.html, Button Styles.dc.html, plus archival notes on the superseded boards. Every claim is labeled verified (checked mechanically or by reading the resulting file) or unproven.

## parity fixes

- README.md, SKILL.md, CLAUDE.md (folder root), Globe.prompt/d.ts context: the docs still taught the single ink etching with "faint meridian lines" while Globe.jsx and the --globe-* tokens ship the three ruled treatments (eclipse, souls, relief) with no grid. The ruling and the code won, all four docs rewritten. The Globe triplet itself (jsx, d.ts, prompt) already agreed and was untouched. Verified.
- README.md ICONOGRAPHY, index line and completion-pack line: taught constructed-div icons while Icons.jsx says span-glyphs were retired for real Lucide paths on the owner's word. The ruling and the code won, README rewritten (world objects like folders and the friend diamond stay hand-built, they are objects, not icons). Verified.
- Dock.jsx vs the icons ruling: friends, library and settings tabs were still span-built glyphs. The ruling won, they are now inlined Lucide paths (users, library, settings-2) at 2px round-cap strokes on the 24 grid. Home stays the logo per README. Dock.d.ts updated to match. Verified in code, look unproven (not mounted).
- Notice.jsx LiveActivity: hardcoded width 326 while layout.css --notice-w is 330 and Banner already used the token. Token won, LiveActivity now uses var(--notice-w). Verified.
- Progress.jsx PingRing default 30 vs --ping-size 22 and the ping ruling. Token and ruling won, default is 22, Progress.d.ts, Progress.prompt.md and the gallery mount hint updated. Verified.
- components.css Permit block vs Permit.jsx: tokens still described the old ID-card permit (--permit-rule document rule, dashed photo frame comment, 700 26px name) while the shipped keepsake has no rules, no frame and a 600 22px name. The corrected component won, tokens realigned and --permit-rule removed (referenced nowhere, grep verified). Permit.jsx now consumes --permit-name. Verified.
- typography.css --font-data comment said "model ids, permit №". № is retired including the permit and raw model ids are banned words, comment now says mind names. Verified.
- Spec board hero: claimed "v3" and "Prototypes are ground truth". CHANGELOG and README caveat say later rulings win. Board now stamped v4 and states the ruling order. Verified.
- Spec board section 01: claimed "no accent family at all" and "earth ramp removed" while colors.css keeps the sand accent pair (--accent-wash, --accent-ink) per README v3.1. Tokens won, board text now describes the accent pair. Verified.
- Spec board section 05: the fourth demo rendered the superseded color globe (blue sea, green and orange lands, blue halo) captioned "shipped as Globe", contradicting Globe.jsx. Code and ruling won, replaced with three monochrome demos matching the eclipse, souls and relief token values exactly, and the paragraph rewritten. Verified against token values, look unproven.
- Spec board place card microlabel: uppercase ui-monospace at .12em tracking, breaking the retired-monospace rule, the .08em tracking cap and the lowercase microlabel recipe in Society.jsx and --place-microlabel. Tokens won. Verified.
- Component Gallery: all mounts checked against current props (Globe variants eclipse, souls, relief present, SealedNote by/count, PlaceCard name/region/blurb, AgentMark guild/trust). Only drift found was the PingRing size hint, updated to 22. Verified.
- Button Styles subtitle: still framed the board as an open choice. It records a made decision (1b chosen and rolled through), subtitle now says so. Verified.

## tells fixed

- old-apple surfaces (heavy hairline rings): spec board card rings at rgba(0,0,0,.1) on paper (live activity, banner, one-mode card, sealed note) and all eight Button Styles card frames, lightened to .05 per --paper-ring. Finding divider .1 aligned to the .09 divider token. Verified.
- old-apple surfaces (inset rings on light surfaces): spec board color swatch outlines were inset box-shadows, now plain 1px rings at .05. Verified.
- bureaucratic forms (document rule lines, dashed photo frames): --permit-rule token and the dashed-frame comment removed from components.css. Permit.jsx itself was already clean. Verified.
- bureaucratic forms (raw model ids in consumer copy): "claude-sonnet-4-5" removed from the spec board metadata tag demo, Chip.prompt.md demo and Chip.d.ts comment, all now Playground Core. Verified.
- fake constructed icons: Dock.jsx span-built friends, library and settings glyphs replaced with real Lucide paths. Docs that taught the constructed doctrine (README, Icons.d.ts, Icons.prompt.md, Dock.d.ts, spec board assets text) rewritten. Verified.
- numbered-record naming: "Field Note №3" removed from ListRow.prompt.md, "permit №" removed from typography.css. Grep over design-system/ and the three live boards finds zero № remaining. Verified.
- grids on world objects: no meridian or lat/long lines remain on any globe in the system (the etch token group is already gone from components.css, MiniGlobe is eclipse at chip scale, spec board demos are grid free). Doc references to meridians removed. Verified by grep.
- oversized pings: PingRing default now 22px, pgPingRing spread is 1.55 (already compliant). The spec board ping demo was already 22px. Verified.
- names: Quill removed from ListRow.prompt.md (now Moth) and Homecoming.d.ts plus Homecoming.prompt.md source labels (now lumen). Remaining demo names in the system are Sol, Lumen, Moth, all on the allowed list. Verified by grep.
- whimsy voice, software words in UI copy, uppercase label grids, saturated hero count: this historical sweep treated tracked uppercase section labels at 11px as sanctioned. that call was superseded on 2026-07-26 by the hard treatment ban and the live labels were restyled.

## boards lacking archival notes (before this pass)

All of these lacked a note and one was added in this pass:
- Playground.dc.html
- Playground v2.dc.html
- Playground v3.dc.html
- Playground v4.dc.html
- Playground iOS v2.dc.html
- Playground iOS v3 - Night.dc.html
- Globe Concept.dc.html
- Design System Versions.dc.html

Already carried one: Playground Screens.dc.html ("the abandoned night draft, kept for comparison"). Not superseded, no note added: Playground iOS.dc.html (named prototype source in README), Tear Options and Naming Screen Options (still-live explorations, excluded per instruction).

## qa results

- esbuild: all 23 components/*.jsx pass npx esbuild --loader:.jsx=jsx after the edits (Dock, Progress, Notice, Permit re-checked post-edit). Verified.
- undefined vars: zero var(--x) references without a definition across components, tokens, styles.css, the spec board, the gallery and Button Styles (scanned before and after the edits, both clean). Verified.
- assets: every local path referenced by design-system files and the two live boards resolves on disk (support.js, styles.css, the seven token files, four marks plus logo-cutout, the three canonical world images). world-coast, world-hall, world-seminar and world-street-jazz are referenced only by the archived Tear Options board and exist. Verified.
- overflow risks on the spec board: one real one found, the section 05 world row grew to six 120px tiles (about 940px) in a roughly 790px column with no wrap, fixed with flex-wrap. The three fixed-height 150px surface demos were arithmetic-checked (modal sheet needs about 53px in 93px, parallel card about 41px in 114px, scroll-edge card about 111px of 150px), no overflow. Gallery hint-size boxes are layout hints, not clips. Arithmetic verified, rendered look unproven.

## untested

- No component was mounted in a browser after the Dock icon rebuild, the Lucide glyphs are syntax-checked and path data is from Lucide, but rendered look at 17px inside the 40px tab is unproven.
- The three new spec-board globe demos copy the token gradient and shadow values verbatim, side-by-side visual parity with the live Globe.jsx render is unproven.
- The archival ribbons were inserted programmatically after each board's opening container div and the insertion was echo-verified, but only Design System Versions was seen rendered. Overlap with each board's own top chrome is unproven.
- The DC runtime (support.js x-import) behavior for the edited boards was not exercised end to end.
- PRODUCT.md line 30 still says "an archivist named quill". It is the constitution and sits outside the fix scope given, flagged for the owner instead of edited.
- AgentState's out ring animates pgPingRing around the full 34px mark. Read here as a state ring on the mark rather than the 22px ping marker, left untouched, owner call unproven.
