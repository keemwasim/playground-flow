Bottom navigation for iOS. Selected tab fills ink with white glyph; the Home glyph is the logo (its hole flips to match the fill). Position: centered, 24px above the home indicator, z --z-dock.

```jsx
<Dock active={tab} onChange={setTab} />
```
