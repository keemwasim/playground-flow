Always in a row of three, always plain counts. Numbers are a diary of the bond, never analytics.

```jsx
<div style={{ display: 'flex', gap: 9 }}>
  <StatCard value={7} label="Trips" />
  <StatCard value={4} label="Friends" />
  <StatCard value={12} label="Field notes" />
</div>
```
