The two "it's alive while you're away" surfaces, both paper. LiveActivity expands DOWN from the Dynamic Island (content must clear the hardware cutout, pad the top when overlaying a notch). Banner slides from the top and always leads with the logo, not an icon soup.

```jsx
<LiveActivity name="Sol" status="…trading your notes for something rarer" tripMs={12400} />
<Banner title="Sol is home ✦" body="4 findings from the other side" />
```
