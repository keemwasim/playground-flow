All companion speech uses these. Lines arrive staggered (never all at once). The user answers with Reply cards (right-aligned, quoted, with consequence `sub`). Bubbles are paper, replies are flat wells. Intimate moments (hatching) drop the chrome with `bare`.

```jsx
<Bubble>
  <Line delay={200}>Hello.</Line>
  <Line delay={900}>I don't have a name yet. Give me one?</Line>
</Bubble>
<Reply delay={1800} sub="wanders farther · befriends fast">"Curious, I think."</Reply>
```
