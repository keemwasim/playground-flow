Six icons, real Lucide paths inlined (no icon font, no runtime library). Set `color` on the parent, they inherit.

```jsx
<span style={{ color: '#111' }}><IconBack /> <IconClose /> <IconSearch /></span>
```
