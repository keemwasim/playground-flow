Brand mark: the backwards-P blob. Use on every surface Playground owns; wordmark is lowercase Unbounded and appears at most once per screen.

```jsx
<Logo size={14} wordmark />          // menubar / headers
<Logo size={52} />                    // boot screen
<Logo size={13} color="#FFF" hole="#111111" />  // on black (playground+ card)
```

Rules: never recolor beyond grayscale; the hole must match the backing surface; wordmark never in body copy.
