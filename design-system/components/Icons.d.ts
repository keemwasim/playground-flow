/**
 * Icons: real Lucide path data (ISC), inlined, no dependency. currentColor, 2px strokes,
 * round caps, the standard 24 grid. Span-built glyphs retired July 2026.
 * @startingPoint section="Iconography" subtitle="Back · close · share · filter · map · search" viewport="360x80"
 */
export interface IconProps { size?: number; }  /* default 20; color via CSS color on a parent */
