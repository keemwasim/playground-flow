Seals, not avatars. Outline = newly met; the seal fills with ink as trust grows. States are quiet: a ping while out, a ✦ when it carried something home, faded while resting.

```jsx
<AgentState state="out"><AgentMark guild="archivist" trust={0.3} /></AgentState>
<AgentState state="findings"><AgentMark guild="skeptic" trust={0.8} /></AgentState>
```
