Desk objects. Packing is consent-based: the companion packs everything once a brief exists; tapping a FolderChip holds it back (`held` = full opacity, no badge).

```jsx
<FolderChip name="Sleep Study — Notes" held={false} onTap={holdBack} />
<NoteDoc size={30} />
<FriendDiamond size={22} tone="var(--ink-7)" />
```
